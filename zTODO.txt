ENGINE
 + MAIN
   - add fullscreen option (main.cpp)
     - if (fullscreen) /*glutEnterGameMode ()*/; else {glutCreateWindow ("game engine"); createMenus ();}
   - Implement Configuration Files
   - Implement Menu
   - Implement Console
   - Optimize Text Drawing
 + TEXTURES
   - possibly make textures ONLY stored in external file instead of directly with map file?
 + POOL
   - temporarily added insideOf() for proper draw order
   - implement ray function for tinting sprite: http://erich.realtimerendering.com/ptinpoly/
 + SKYBOX
   - fix implementation (get Ian's getPosition code)
 + ENVIRONMENT
   - implement colour object and divide by 255 for proper colour

BUILDER 2.0
 + Implement Worldcraft *.map reading
 + Fix output directory structure being non-existant
 + Possibly allow textures mid-animation to be selected
